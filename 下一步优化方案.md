# N10P 项目下一步优化方案

> 创建: 2026-07-13 | EKF 滤波已验证通过，以下为后续优化建议

---

## 一、立即可做的简化

### 1.1 让 EKF + 有线成为默认

当前命令:
```bash
ros2 launch n10p_bringup n10p_bringup_launch.py scan_source:=wired us1e_ekf:=true
```

优化后:
```bash
ros2 launch n10p_bringup n10p_bringup_launch.py
```

改动: 将 `use_ekf` 默认值从 `false` 改为 `true`。

**风险**: 如果用户某次没接飞控，EKF 节点会启动但收不到 /imu 数据（ano_bridge 不在线），滤波器会用初始四元数 [1,0,0,0]（水平朝前）一直输出。对 SLAM 的影响：姿态始终为水平，如果你手持时倾斜了雷达→激光平面歪了→建图可能变形。

**安全做法**: 检测 /imu 话题是否有数据，超过 5 秒无数据则自动退回透传模式。

### 1.2 一键建图 launch

封装一个 `slam_ekf_launch.py`:
```bash
ros2 launch n10p_slam slam_ekf_launch.py
# 一条命令 = bringup(带EKF) + slam_only
```

内部用 `IncludeLaunchDescription` 嵌套两个 launch。

### 1.3 一键导航 launch

同理封装 `nav_ekf_launch.py`:
```bash
ros2 launch n10p_nav nav_ekf_launch.py map:=/path/to/map.yaml
# 一条命令 = bringup(带EKF) + nav_only
```

---

## 二、代码质量优化

### 2.1 imu_filter_node 的 shutdown 修复需要同步到 ano_bridge_node

当前 `ano_bridge_node.py` 的 shutdown 仍然有同样的问题（后台串口线程在 DDS context 销毁后还试图 publish）。修复方案和 imu_filter_node 一样：catch ExternalShutdownException + 检查 rclpy.ok()。

### 2.2 清理 dead code

以下代码/文件可考虑移除（不紧急，但整洁）:
- `n10p_fusion/config/ekf.yaml` 中遗留的 robot_localization 参数（已不用）
- `esp32_n10p_bridge/` — 如果确定不用无线模式

### 2.3 n10p_fusion 包的 entry_points 路径修复自动化

每次 `colcon build` 后需要手动 `cp bin/... lib/n10p_fusion/...`（Bug 11）。
可选方案: 改 launch 文件使用 `bin/` 路径，或添加 post-build hook。

---

## 三、功能增强

### 3.1 EKF 自检

滤波器启动时发布一个 `/ekf_status` 话题（std_msgs/String），报告:
- "initializing" — 等待首帧 IMU 数据
- "running" — 正常滤波中
- "no_imu" — 超过 2 秒没收到 IMU（可能飞控断联）

### 3.2 自适应 alpha

当前 alpha=0.02 是固定值。可以通过 IMU 角速度的方差动态调整:
- 静止时增大 alpha（更信飞控四元数，减少 IMU 噪声）
- 快速旋转时减小 alpha（更信 IMU 陀螺仪积分）

### 3.3 加速度融合（当前未启用）

当前滤波器只融合了角速度+姿态，没有融合加速度。
加速度可以用于:
- 速度估计修正（配合飞控 0x07 帧）
- 检测起飞/着陆（Z 轴加速度突变）

---

## 四、长期规划

### 4.1 飞控 0xF5 下行帧集成

当前 `ano_bridge_node` 已有 `_send_position_downlink()` 逻辑（订阅 /amcl_pose → 打包 0xF5 → 串口发送）。
滤波器 `/odometry/filtered` 的姿态更平滑，可以考虑用滤波后的姿态打包 0xF5 帧，而非原始飞控姿态。

### 4.2 K230 视觉融合

`ano_bridge_node` 已预留 `_build_visual_frame()` 占位。
当 K230 接入后，DX/DY/DZ 从 `/k230_detection` 话题读取，叠加到 AMCL 位置 → 打包 0xF5。

### 4.3 树莓派性能剖析

当前所有节点都跑在树莓派上。后续可以考虑:
- slam_toolbox 是否占满 CPU → 降分辨率或降频率
- imu_filter_node 占多少 CPU → 优化四元数乘法
- 整体延迟: 从激光采集到 /cmd_vel 输出的端到端延迟

---

## 五、优先级排序

| 优先级 | 项目 | 工作量 | 影响 |
|:---:|------|:---:|------|
| P0 | **use_ekf 改为默认 true** | 改一行 | 命令行简化 50% |
| P0 | **一键建图 + 一键导航 launch** | 新建2个文件 | 用户体验大幅提升 |
| P1 | ano_bridge shutdown 修复 | 改5行 | 减少残留进程 |
| P1 | EKF 自检话题 | 加20行 | 调试体验提升 |
| P2 | 自适应 alpha | 加30行 | 滤波质量提升 |
| P2 | 清理 dead code | 删文件 | 代码整洁 |
| P3 | 加速度融合 | 加50行 | 速度估计更准 |
| P3 | 性能剖析 | 测试 | 找到瓶颈 |
