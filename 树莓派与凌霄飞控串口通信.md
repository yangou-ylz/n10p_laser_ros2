# 凌霄飞控 IMU 数据接入指南
## 树莓派 UART 驱动 + 匿名协议全帧解析器

**适用场景**：树莓派通过硬件串口接入 STM32F407 飞控的 UART5 总线，实时接收 IMU
姿态、高度、速度等数据，并预留发送自定义帧的能力。

---

## 目录

1. [硬件连接](#1-硬件连接)
2. [通信参数](#2-通信参数)
3. [帧率说明（重要）](#3-帧率说明重要)
4. [协议格式完整规范](#4-协议格式完整规范)
5. [所有帧定义](#5-所有帧定义)
6. [现成 Python 解析库](#6-现成-python-解析库)
7. [使用示例](#7-使用示例)
8. [发送自定义帧](#8-发送自定义帧)
9. [未来扩展指引](#9-未来扩展指引)

---

## 1. 硬件连接

STM32F407 的 **UART5** 是飞控与凌霄 IMU 模块之间的通信总线。树莓派通过并联
方式接入此总线，**不需要中断或修改飞控固件**。

```
凌霄IMU模块
    │
    │ 500000 baud 8N1
    ▼
STM32F407
  PC12 = UART5_TX  (STM32 → IMU 方向，发送遥控/控制帧)
  PD2  = UART5_RX  (IMU  → STM32 方向，发送姿态/速度/状态帧)
    │
    ├── PD2 (UART5_RX) ────────────── 树莓派 RXD (GPIO15)
    │                                （接此线可读 IMU 主动上报的所有数据帧）
    │
    └── PC12 (UART5_TX) ─── [可选] ─ 树莓派 第二个RXD 或逻辑分析仪
                                     （接此线可读 STM32 发给 IMU 的帧）
    
GND ────────────────────────────────── 树莓派 GND（必须共地）
```

**最小接法（推荐，只读 IMU 主动上报数据）**：

| 飞控引脚 | 树莓派引脚 | 说明 |
|---------|-----------|------|
| PD2 (UART5_RX) | GPIO15 (RXD) | IMU→STM32 数据（姿态/速度/高度等） |
| GND | GND | 共地，必须接 |

> ⚠ **注意电平**：STM32 GPIO 为 3.3V 逻辑电平，树莓派 GPIO 也是 3.3V，
> **直接连接即可**，无需电平转换器。
> 如果飞控 GND 与树莓派 GND 已通过电源共地，可省略 GND 线。

---

## 2. 通信参数

```
波特率:  500000 bps (500K)
数据位:  8
停止位:  1
校验位:  无 (None)
流控:    无 (无 CTS/RTS)
```

树莓派端 `/boot/config.txt` 需禁用蓝牙串口（如使用 `/dev/ttyAMA0`）：

```ini
# /boot/config.txt
dtoverlay=disable-bt
enable_uart=1
```

或使用 USB 转串口模块（CH340/CP2102），只需调整端口号，波特率同上。

```python
# 打开串口示例
import serial
ser = serial.Serial('/dev/ttyAMA0', 500000, timeout=0.01)
# USB串口: serial.Serial('/dev/ttyUSB0', 500000, timeout=0.01)
```

---

## 3. 帧率说明（重要）

**帧率直接决定控制延迟**。各帧的实测发送频率如下：

| 帧ID | 内容 | 发送方 | 实测频率 | 建议 |
|------|------|--------|---------|------|
| `0x04` | **姿态四元数** | IMU→STM32 | **~67 Hz** | ✅ 首选姿态来源 |
| `0x07` | 飞行速度 vx/vy/vz | IMU→STM32 | ~50 Hz | ✅ 正常使用 |
| `0x05` | 融合高度 | IMU→STM32 | ~50 Hz | ✅ 正常使用 |
| `0x06` | 飞控状态(模式/解锁) | IMU→STM32 | ~20 Hz | ✅ 正常使用 |
| `0x01` | 加速度/陀螺仪原始值 | IMU→STM32 | ~100 Hz | ✅ 最高频率 |
| `0x02` | 气压计/磁力计 | IMU→STM32 | ~20 Hz | ✅ 正常 |
| `0x0D` | 电池电压/电流 | IMU→STM32 | ~1 Hz | ✅ 低频正常 |
| **`0x03`** | **姿态欧拉角** | IMU→STM32 | **~0.67 Hz** | ❌ 极低！不要用它做控制 |
| `0x08` | XY 位移 | IMU→STM32 | ~20 Hz | 需外部传感器才有效 |

### 关键结论：使用 0x04 四元数，不要用 0x03 欧拉角

`0x03` 帧虽然直接给出角度，但**每 1.5 秒才发一次（约 0.67 Hz）**，完全不能用
于实时控制。

`0x04` 四元数帧以 ~67 Hz 发送，在 Pi 端自行转换：

```python
import math

def quat_to_euler(w, x, y, z):
    """四元数 → 欧拉角（单位：度）"""
    # Roll (绕X轴)
    sinr_cosp = 2 * (w * x + y * z)
    cosr_cosp = 1 - 2 * (x * x + y * y)
    roll = math.degrees(math.atan2(sinr_cosp, cosr_cosp))
    
    # Pitch (绕Y轴)
    sinp = 2 * (w * y - z * x)
    sinp = max(-1.0, min(1.0, sinp))  # 防溢出
    pitch = math.degrees(math.asin(sinp))
    
    # Yaw (绕Z轴)
    siny_cosp = 2 * (w * z + x * y)
    cosy_cosp = 1 - 2 * (y * y + z * z)
    yaw = math.degrees(math.atan2(siny_cosp, cosy_cosp))
    
    return roll, pitch, yaw
```

转换计算量极小（< 1μs），67 Hz 完全满足实时控制需求。

---

## 4. 协议格式完整规范

### 4.1 帧结构

```
字节偏移:  0      1      2      3      4...(3+N)   4+N    5+N
          ┌──────┬──────┬──────┬──────┬────────────┬──────┬──────┐
          │ 0xAA │ DEST │ CMD  │  LEN │  DATA[LEN] │  SC  │  AC  │
          └──────┴──────┴──────┴──────┴────────────┴──────┴──────┘
```

| 字段 | 字节 | 说明 |
|------|------|------|
| `0xAA` | 1 | 固定帧头，永远是 0xAA |
| `DEST` | 1 | 目标地址 |
| `CMD` | 1 | 帧功能码（帧ID） |
| `LEN` | 1 | DATA 区字节数（不含校验） |
| `DATA` | LEN | 帧数据内容（见各帧定义） |
| `SC` | 1 | Sum Check（和校验） |
| `AC` | 1 | Add Check（附加校验） |

**帧总长度** = LEN + 6 字节

### 4.2 地址定义

| 地址 | 含义 |
|------|------|
| `0xFF` | 广播（所有设备接收） |
| `0x60` | 凌霄 IMU 模块（目标为IMU） |
| `0x61` | STM32 飞控板（目标为飞控） |
| `0xAF` | 上位机/地面站 |
| `0x22` | 匿名光流模块 |
| `0x30` | 匿名 UWB 模块 |

### 4.3 SC/AC 双校验算法

**校验覆盖范围**：从帧头 `0xAA` 开始，共 `LEN + 4` 个字节（不含SC和AC本身）。

```python
def compute_sc_ac(buf, lен):
    """buf 为完整帧字节序列，len 为 DATA 长度（即 buf[3]）"""
    sc = 0
    ac = 0
    for i in range(len + 4):   # 覆盖 0xAA, DEST, CMD, LEN, DATA[0..LEN-1]
        sc = (sc + buf[i]) & 0xFF
        ac = (ac + sc)    & 0xFF
    return sc, ac

def verify_frame(buf):
    """返回 True 表示校验通过"""
    lен = buf[3]
    if len(buf) < lен + 6:
        return False
    sc, ac = compute_sc_ac(buf, lен)
    return sc == buf[lен + 4] and ac == buf[lен + 5]
```

---

## 5. 所有帧定义

> DATA 区所有多字节数值均为**小端序（Little-Endian）**。
> `s16` = 有符号16位整数，`u16` = 无符号16位，`s32` = 有符号32位，`u32` = 无符号32位。

---

### 0x01 — 惯性传感器原始数据

**方向**：IMU → STM32  
**LEN**：13 字节  
**频率**：~100 Hz  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | acc_x | — | X轴加速度（原始量化值） |
| 2 | s16 | acc_y | — | Y轴加速度 |
| 4 | s16 | acc_z | — | Z轴加速度 |
| 6 | s16 | gyr_x | — | X轴角速度（原始量化值） |
| 8 | s16 | gyr_y | — | Y轴角速度 |
| 10 | s16 | gyr_z | — | Z轴角速度 |
| 12 | u8 | shock_flag | — | 振动标志（0=正常） |

---

### 0x02 — 气压计 + 磁力计

**方向**：IMU → STM32  
**LEN**：14 字节  
**频率**：~20 Hz  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | mag_x | — | 磁力计 X |
| 2 | s16 | mag_y | — | 磁力计 Y |
| 4 | s16 | mag_z | — | 磁力计 Z |
| 6 | s32 | baro_alt | cm | 气压高度（厘米） |
| 10 | s16 | temperature | 0.1°C | 温度（×0.1 得摄氏度） |
| 12 | u8 | mag_sta | — | 磁力计状态 |
| 13 | u8 | baro_sta | — | 气压计状态 |

---

### 0x03 — 姿态欧拉角 ⚠ 低频（0.67 Hz），勿用于控制

**方向**：IMU → STM32  
**LEN**：7 字节  
**频率**：约 0.67 Hz（每 1.5 秒一帧）  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | roll | 0.01° | 横滚角（×0.01 得度数） |
| 2 | s16 | pitch | 0.01° | 俯仰角 |
| 4 | s16 | yaw | 0.01° | 偏航角 |
| 6 | u8 | fusion_sta | — | 融合状态（0=正常） |

---

### 0x04 — 姿态四元数 ✅ 首选（67 Hz）

**方向**：IMU → STM32  
**LEN**：9 字节  
**频率**：~67 Hz  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | q_w | 0.0001 | 四元数 W 分量（×0.0001） |
| 2 | s16 | q_x | 0.0001 | 四元数 X 分量 |
| 4 | s16 | q_y | 0.0001 | 四元数 Y 分量 |
| 6 | s16 | q_z | 0.0001 | 四元数 Z 分量 |
| 8 | u8 | fusion_sta | — | 融合状态 |

解码示例：
```python
import struct, math
def decode_0x04(data):
    w, x, y, z = [struct.unpack_from('<h', data, i*2)[0] * 0.0001 for i in range(4)]
    sta = data[8]
    roll  = math.degrees(math.atan2(2*(w*x+y*z), 1-2*(x*x+y*y)))
    sinp  = max(-1, min(1, 2*(w*y-z*x)))
    pitch = math.degrees(math.asin(sinp))
    yaw   = math.degrees(math.atan2(2*(w*z+x*y), 1-2*(y*y+z*z)))
    return w, x, y, z, roll, pitch, yaw, sta
```

---

### 0x05 — 融合高度

**方向**：IMU → STM32  
**LEN**：9 字节  
**频率**：~50 Hz  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s32 | alt_fused | cm | 融合高度（厘米） |
| 4 | s32 | alt_add | cm | 附加高度（激光/超声波补偿后） |
| 8 | u8 | sta | — | 高度融合状态 |

---

### 0x06 — 飞控状态

**方向**：IMU → STM32  
**LEN**：5 字节（最小），实际可能更多  
**频率**：~20 Hz  

| 偏移 | 类型 | 字段 | 说明 |
|------|------|------|------|
| 0 | u8 | fc_mode | 飞行模式：0=姿态 1=定高 2=定点 3=程控 |
| 1 | u8 | unlock_sta | 解锁状态：0=上锁 1=解锁 |
| 2 | u8 | cmd_cid | 当前执行 CMD 的 CID |
| 3 | u8 | cmd_0 | CMD_0 参数 |
| 4 | u8 | cmd_1 | CMD_1 参数 |

---

### 0x07 — 飞行速度

**方向**：IMU → STM32  
**LEN**：6 字节  
**频率**：~50 Hz  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | vel_x | cm/s | X 方向速度（机体系） |
| 2 | s16 | vel_y | cm/s | Y 方向速度 |
| 4 | s16 | vel_z | cm/s | Z 方向速度（正向上） |

---

### 0x08 — XY 位移

**方向**：IMU → STM32  
**LEN**：8 字节  
**频率**：~20 Hz（需外部定位传感器有效才有意义）  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s32 | pos_x | cm | X 位移（相对起飞点） |
| 4 | s32 | pos_y | cm | Y 位移 |

---

### 0x09 — 风速估计

**方向**：IMU → STM32  
**LEN**：4 字节  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | wind_x | cm/s | X 方向风速估计 |
| 2 | s16 | wind_y | cm/s | Y 方向风速估计 |

---

### 0x0A — 目标姿态

**方向**：IMU → STM32  
**LEN**：7 字节（格式同 0x03）  

飞控内部目标姿态，字段同 0x03（roll/pitch/yaw/sta）。

---

### 0x0D — 电池信息

**方向**：IMU → STM32  
**LEN**：4 字节  
**频率**：~1 Hz  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | u16 | voltage | 0.01V | 电池电压（×0.01 得伏特） |
| 2 | u16 | current | 0.01A | 电池电流 |

解码：`volt = struct.unpack('<H', data[0:2])[0] * 0.01`

---

### 0x0E — 外接模块状态

**方向**：IMU → STM32  
**LEN**：4 字节  
**频率**：~2 Hz  

状态码含义：`0`=无数据 `1`=不可用 `2`=正常 `3`=良好  

| 偏移 | u8 | 字段 | 说明 |
|------|-----|------|------|
| 0 | sta_gvel | 通用速度传感器状态 |
| 1 | sta_gpos | 通用位置传感器状态 |
| 2 | sta_gps | GPS 模块状态 |
| 3 | sta_alt | 辅助高度传感器状态 |

---

### 0x20 — 电机 PWM

**方向**：IMU → STM32（或STM32→ESC方向的参考）  
**LEN**：8~16 字节（每电机 2 字节）  

每个 u16 对应一路电机的 PWM 值（单位因配置而异）。  
例：4电机 → LEN=8，`M1,M2,M3,M4 = struct.unpack('<4H', data[0:8])`

---

### 0x21 — 姿态控制量

**方向**：IMU → STM32  
**LEN**：8 字节  

| 偏移 | 类型 | 字段 | 说明 |
|------|------|------|------|
| 0 | s16 | ctrl_roll | Roll 控制量 |
| 2 | s16 | ctrl_pitch | Pitch 控制量 |
| 4 | s16 | ctrl_yaw | Yaw 控制量 |
| 6 | s16 | ctrl_thr | 油门控制量 |

---

### 0x40 — 遥控器数据

**方向**：STM32 → IMU  
**LEN**：20 字节  
**频率**：~50 Hz  

包含 10 个通道，每通道 s16，单位 μs（通常 1000~2000）：

| 字节偏移 | 通道 | 含义 |
|---------|------|------|
| 0-1 | CH1 | Roll |
| 2-3 | CH2 | Pitch |
| 4-5 | CH3 | Throttle |
| 6-7 | CH4 | Yaw |
| 8-9 | CH5/AUX1 | 飞行模式切换 |
| 10-11 | CH6/AUX2 | 用户自定义 |
| 12-13 | CH7/AUX3 | 用户自定义 |
| 14-15 | CH8/AUX4 | 用户自定义 |
| 16-17 | CH9/AUX5 | 用户自定义 |
| 18-19 | CH10/AUX6 | 用户自定义 |

AUX1 飞行模式含义：

| AUX1 值 | 飞行模式 |
|---------|---------|
| 1000~1200 | 模式1（定高+姿态） |
| 1200~1400 | 失控保护区间（避免设置） |
| 1400~1600 | 模式2（定点） |
| 1600~1800 | 失控保护区间（避免设置） |
| 1800~2000 | 模式3（程控） |

---

### 0x41 — 实时控制帧（程控模式专用）

**方向**：STM32 → IMU  
**LEN**：≥14 字节  
**频率**：50 Hz（程控模式 3 下持续发送）  

| 偏移 | 类型 | 字段 | 单位 | 说明 |
|------|------|------|------|------|
| 0 | s16 | roll | 0.01° | 目标横滚角 |
| 2 | s16 | pitch | 0.01° | 目标俯仰角 |
| 4 | s16 | thr | 0.1% | 油门百分比 |
| 6 | s16 | yaw_rate | °/s | 偏航角速度 |
| 8 | s16 | vel_x | cm/s | X 速度（定点模式用） |
| 10 | s16 | vel_y | cm/s | Y 速度 |
| 12 | s16 | vel_z | cm/s | Z 速度 |

---

### 0xA0 — 字符串 LOG

**方向**：IMU → STM32 或 STM32 → 上位机  
**LEN**：可变  

| 偏移 | 类型 | 说明 |
|------|------|------|
| 0 | u8 | 颜色：0=黑 1=红 2=绿 |
| 1~LEN-1 | ASCII字符串 | 日志文本（GBK编码） |

---

### 0xE0 — CMD 命令帧

**方向**：STM32 → IMU（或上位机 → STM32）  
**LEN**：3 字节  

| 偏移 | u8 | 说明 |
|------|-----|------|
| 0 | CID | 命令类别 |
| 1 | CMD_0 | 命令参数0 |
| 2 | CMD_1 | 命令参数1 |

常用命令（CID=0x10 飞行控制类）：

| CID | CMD_0 | CMD_1 | 功能 |
|-----|-------|-------|------|
| 0x10 | 0x01 | 0 | 解锁 |
| 0x10 | 0x02 | 0 | 上锁 |
| 0x10 | 0x03 | 0 | 一键起飞 |
| 0x10 | 0x04 | 0 | 一键降落 |
| 0x10 | 0x05 | 0 | 急停/刹车 |

---

### 0xE2 — 参数写入

**方向**：上位机 → IMU  
**LEN**：可变  

用于修改 IMU 内部参数（如 PID、报文频率等），具体参数 ID 见凌霄技术手册。

---

### 0x00 — CK 返回帧

**方向**：IMU → STM32（对每条指令的应答）  
**LEN**：3 字节  

| 偏移 | u8 | 说明 |
|------|-----|------|
| 0 | for_cmd | 被应答帧的 CMD |
| 1 | sc | 原始帧的 SC |
| 2 | ac | 原始帧的 AC |

---

### 0x30/0x32/0x33/0x34 — 外部传感器上报

**方向**：STM32 → IMU（将外接传感器数据上报给 IMU 做融合）  

| CMD | 功能 |
|-----|------|
| 0x32 | 通用位置上报（x/y/z cm，精度信息） |
| 0x33 | 通用速度上报（vx/vy/vz cm/s） |
| 0x34 | 通用测距上报（单路距离 cm） |

---

## 6. 现成 Python 解析库

下面是完整的、库化的 Python 解析器，直接可在树莓派上运行。
**已在 Windows/CH340 环境验证，逻辑与树莓派完全一致**（仅需改串口名）。

文件：`ano_protocol.py`

```python
# -*- coding: utf-8 -*-
"""
ano_protocol.py — 凌霄匿名协议完整解析库
用法：
    from ano_protocol import AnoProtocol
    proto = AnoProtocol('/dev/ttyAMA0', 500000)
    proto.start()                        # 启动后台接收线程
    
    # 阻塞等待，超时 0.1 秒
    frame = proto.get_frame(0x04, timeout=0.1)   # 拿最新四元数帧
    if frame:
        w, x, y, z = frame['w'], frame['x'], frame['y'], frame['z']
    
    proto.stop()
"""

import serial
import struct
import threading
import time
import math
from collections import defaultdict


# ── 地址定义 ────────────────────────────────────────────────────
ADDR = {
    0xFF: 'broadcast',
    0x60: 'IMU',
    0x61: 'STM32',
    0xAF: 'PC',
}

# ── 帧名称 ──────────────────────────────────────────────────────
FRAME_NAME = {
    0x00: 'CK_Reply',
    0x01: 'IMU_Raw',
    0x02: 'Baro_Mag',
    0x03: 'Euler_Angle',
    0x04: 'Quaternion',
    0x05: 'Fused_Alt',
    0x06: 'FC_Status',
    0x07: 'Velocity',
    0x08: 'XY_Pos',
    0x09: 'Wind',
    0x0A: 'Target_Att',
    0x0D: 'Battery',
    0x0E: 'Module_Status',
    0x20: 'Motor_PWM',
    0x21: 'Att_Ctrl',
    0x32: 'Pos_Report',
    0x33: 'Vel_Report',
    0x34: 'Dist_Report',
    0x40: 'RC_Data',
    0x41: 'RT_Ctrl',
    0xA0: 'Log_String',
    0xE0: 'CMD',
    0xE2: 'Param_Write',
}


def _s16(b, off): return struct.unpack_from('<h', b, off)[0]
def _u16(b, off): return struct.unpack_from('<H', b, off)[0]
def _s32(b, off): return struct.unpack_from('<i', b, off)[0]
def _u32(b, off): return struct.unpack_from('<I', b, off)[0]


def verify_frame(buf):
    """校验帧完整性，buf 为 bytes/bytearray，返回 True=通过"""
    if len(buf) < 6 or buf[0] != 0xAA:
        return False
    ln = buf[3]
    if len(buf) < ln + 6:
        return False
    sc = ac = 0
    for i in range(ln + 4):
        sc = (sc + buf[i]) & 0xFF
        ac = (ac + sc)    & 0xFF
    return sc == buf[ln + 4] and ac == buf[ln + 5]


def build_frame(dest, cmd, data: bytes) -> bytes:
    """
    构造一帧（含校验），返回完整帧 bytes。
    dest: 目标地址 (0xFF/0x60/0x61...)
    cmd:  帧功能码
    data: DATA 区 bytes
    """
    ln   = len(data)
    buf  = bytearray([0xAA, dest & 0xFF, cmd & 0xFF, ln & 0xFF])
    buf += bytearray(data)
    sc = ac = 0
    for b in buf:
        sc = (sc + b) & 0xFF
        ac = (ac + sc) & 0xFF
    buf += bytearray([sc, ac])
    return bytes(buf)


def decode_frame(cmd: int, data: bytes) -> dict:
    """
    将 DATA 区字节解码为字典。
    返回值永远是 dict，未知帧返回 {'raw': hex字符串}。
    所有解码失败时返回 {'error': 'msg', 'raw': hex}
    """
    p = data
    n = len(p)
    try:
        # ── 0x01 惯性传感器 ──────────────────────────────────────
        if cmd == 0x01 and n >= 13:
            return {
                'acc_x': _s16(p, 0), 'acc_y': _s16(p, 2), 'acc_z': _s16(p, 4),
                'gyr_x': _s16(p, 6), 'gyr_y': _s16(p, 8), 'gyr_z': _s16(p, 10),
                'shock': p[12],
            }
        # ── 0x02 气压/磁力 ───────────────────────────────────────
        elif cmd == 0x02 and n >= 12:
            return {
                'mag_x': _s16(p, 0), 'mag_y': _s16(p, 2), 'mag_z': _s16(p, 4),
                'baro_alt_cm': _s32(p, 6),
                'temp_c': _s16(p, 10) * 0.1,
            }
        # ── 0x03 欧拉角（低频，仅供参考） ─────────────────────────
        elif cmd == 0x03 and n >= 7:
            return {
                'roll_deg':  _s16(p, 0) * 0.01,
                'pitch_deg': _s16(p, 2) * 0.01,
                'yaw_deg':   _s16(p, 4) * 0.01,
                'fusion_sta': p[6],
            }
        # ── 0x04 四元数（推荐！67 Hz） ───────────────────────────
        elif cmd == 0x04 and n >= 9:
            w = _s16(p, 0) * 0.0001
            x = _s16(p, 2) * 0.0001
            y = _s16(p, 4) * 0.0001
            z = _s16(p, 6) * 0.0001
            # 在此直接算欧拉角，减少外部调用
            roll  = math.degrees(math.atan2(2*(w*x+y*z), 1-2*(x*x+y*y)))
            sinp  = max(-1.0, min(1.0, 2*(w*y-z*x)))
            pitch = math.degrees(math.asin(sinp))
            yaw   = math.degrees(math.atan2(2*(w*z+x*y), 1-2*(y*y+z*z)))
            return {
                'w': w, 'x': x, 'y': y, 'z': z,
                'roll_deg': roll, 'pitch_deg': pitch, 'yaw_deg': yaw,
                'fusion_sta': p[8],
            }
        # ── 0x05 融合高度 ────────────────────────────────────────
        elif cmd == 0x05 and n >= 9:
            return {
                'alt_fused_cm': _s32(p, 0),
                'alt_add_cm':   _s32(p, 4),
                'sta': p[8],
            }
        # ── 0x06 飞控状态 ────────────────────────────────────────
        elif cmd == 0x06 and n >= 5:
            mode_map = {0: 'attitude', 1: 'alt_hold', 2: 'pos_hold', 3: 'mission'}
            return {
                'mode':     p[0],
                'mode_str': mode_map.get(p[0], f'unknown({p[0]})'),
                'unlocked': bool(p[1]),
                'cmd_cid':  p[2],
                'cmd_0':    p[3],
                'cmd_1':    p[4],
            }
        # ── 0x07 飞行速度 ────────────────────────────────────────
        elif cmd == 0x07 and n >= 6:
            return {
                'vel_x_cms': _s16(p, 0),
                'vel_y_cms': _s16(p, 2),
                'vel_z_cms': _s16(p, 4),
            }
        # ── 0x08 XY位移 ──────────────────────────────────────────
        elif cmd == 0x08 and n >= 8:
            return {
                'pos_x_cm': _s32(p, 0),
                'pos_y_cm': _s32(p, 4),
            }
        # ── 0x09 风速 ────────────────────────────────────────────
        elif cmd == 0x09 and n >= 4:
            return {
                'wind_x_cms': _s16(p, 0),
                'wind_y_cms': _s16(p, 2),
            }
        # ── 0x0D 电池 ────────────────────────────────────────────
        elif cmd == 0x0D and n >= 4:
            return {
                'voltage_v':  _u16(p, 0) * 0.01,
                'current_a':  _u16(p, 2) * 0.01,
            }
        # ── 0x0E 外接模块状态 ────────────────────────────────────
        elif cmd == 0x0E and n >= 4:
            sta_str = {0: 'no_data', 1: 'unavail', 2: 'ok', 3: 'good'}
            return {
                'sta_gvel': sta_str.get(p[0], '?'),
                'sta_gpos': sta_str.get(p[1], '?'),
                'sta_gps':  sta_str.get(p[2], '?'),
                'sta_alt':  sta_str.get(p[3], '?'),
            }
        # ── 0x20 电机PWM ─────────────────────────────────────────
        elif cmd == 0x20 and n >= 2:
            n_motors = n // 2
            return {f'M{i+1}': _u16(p, i*2) for i in range(n_motors)}
        # ── 0x40 遥控器 ──────────────────────────────────────────
        elif cmd == 0x40 and n >= 20:
            names = ['roll','pitch','throttle','yaw',
                     'aux1','aux2','aux3','aux4','aux5','aux6']
            return {names[i]: _s16(p, i*2) for i in range(10)}
        # ── 0x41 实时控制 ────────────────────────────────────────
        elif cmd == 0x41 and n >= 14:
            return {
                'roll_deg':  _s16(p, 0) * 0.01,
                'pitch_deg': _s16(p, 2) * 0.01,
                'thr_pct':   _s16(p, 4) * 0.1,
                'yaw_rate':  _s16(p, 6),
                'vel_x_cms': _s16(p, 8),
                'vel_y_cms': _s16(p, 10),
                'vel_z_cms': _s16(p, 12),
            }
        # ── 0xA0 日志字符串 ──────────────────────────────────────
        elif cmd == 0xA0 and n >= 1:
            color = {0: 'black', 1: 'red', 2: 'green'}.get(p[0], '?')
            return {
                'color': color,
                'text':  p[1:].decode('utf-8', errors='replace').rstrip('\x00'),
            }
        # ── 0x00 CK 应答 ─────────────────────────────────────────
        elif cmd == 0x00 and n >= 3:
            return {'for_cmd': p[0], 'sc': p[1], 'ac': p[2]}
        # ── 0xE0 CMD ─────────────────────────────────────────────
        elif cmd == 0xE0 and n >= 3:
            return {'cid': p[0], 'cmd_0': p[1], 'cmd_1': p[2]}

    except Exception as e:
        return {'error': str(e), 'raw': p.hex()}

    # 未知帧 / 长度不足
    return {'raw': p.hex()}


class AnoProtocol:
    """
    凌霄匿名协议驱动类。
    
    用法：
        proto = AnoProtocol('/dev/ttyAMA0', 500000)
        proto.start()
        
        while True:
            f = proto.get_frame(0x04)          # 阻塞直到拿到四元数帧
            print(f['roll_deg'], f['pitch_deg'], f['yaw_deg'])
        
        proto.stop()
    """

    def __init__(self, port: str, baudrate: int = 500000):
        self._ser     = serial.Serial(port, baudrate, timeout=0.02)
        self._buf     = bytearray()
        self._lock    = threading.Lock()
        self._frames  = {}          # {cmd: (decoded_dict, raw_bytes, timestamp)}
        self._callbacks = defaultdict(list)  # {cmd: [callable, ...]}
        self._running = False
        self._thread  = None
        self._stats   = defaultdict(int)     # {cmd: count}
        self._err_cnt = 0

    # ── 控制 ────────────────────────────────────────────────────

    def start(self):
        """启动后台接收解析线程"""
        self._running = True
        self._thread  = threading.Thread(target=self._recv_loop, daemon=True)
        self._thread.start()

    def stop(self):
        """停止接收线程，关闭串口"""
        self._running = False
        if self._thread:
            self._thread.join(timeout=2.0)
        self._ser.close()

    # ── 数据获取 ─────────────────────────────────────────────────

    def get_frame(self, cmd: int, timeout: float = None) -> dict | None:
        """
        获取指定 CMD 帧的最新解码数据。
        timeout=None → 立即返回（可能为 None）
        timeout=0.1  → 最多等 0.1 秒，还没数据返回 None
        """
        deadline = time.monotonic() + timeout if timeout else None
        while True:
            with self._lock:
                entry = self._frames.get(cmd)
            if entry:
                return entry[0]   # decoded dict
            if deadline is None or time.monotonic() >= deadline:
                return None
            time.sleep(0.002)

    def get_frame_raw(self, cmd: int) -> bytes | None:
        """返回最新一帧的原始字节（含帧头和校验），用于转发"""
        with self._lock:
            entry = self._frames.get(cmd)
        return entry[1] if entry else None

    def get_frame_age(self, cmd: int) -> float | None:
        """返回该帧距今多少秒，None 表示从未收到"""
        with self._lock:
            entry = self._frames.get(cmd)
        return time.monotonic() - entry[2] if entry else None

    def wait_frame(self, cmd: int, timeout: float = 2.0) -> dict | None:
        """阻塞等待，直到收到指定帧或超时"""
        return self.get_frame(cmd, timeout=timeout)

    def stats(self) -> dict:
        """返回各帧接收计数统计"""
        with self._lock:
            return dict(self._stats), self._err_cnt

    # ── 回调注册 ─────────────────────────────────────────────────

    def register_callback(self, cmd: int, fn):
        """
        注册帧回调。每次收到 cmd 帧时，自动调用 fn(decoded_dict)。
        注意：回调在接收线程中执行，避免阻塞操作。
        """
        self._callbacks[cmd].append(fn)

    # ── 发送 ─────────────────────────────────────────────────────

    def send(self, dest: int, cmd: int, data: bytes = b'') -> None:
        """发送任意帧"""
        frame = build_frame(dest, cmd, data)
        self._ser.write(frame)

    def send_cmd(self, cid: int, cmd_0: int, cmd_1: int = 0) -> None:
        """发送 0xE0 CMD 帧（目标地址 0xFF 广播）"""
        self.send(0xFF, 0xE0, bytes([cid, cmd_0, cmd_1]))

    def send_ctrl(self, roll_deg: float, pitch_deg: float,
                  thr_pct: float, yaw_rate: int,
                  vel_x: int = 0, vel_y: int = 0, vel_z: int = 0) -> None:
        """
        发送 0x41 实时控制帧（需飞控处于程控模式 mode=3）。
        roll/pitch 单位: 度   thr_pct: 0~100   yaw_rate: 度/秒   vel: cm/s
        """
        data = struct.pack('<7h',
                           int(roll_deg * 100),
                           int(pitch_deg * 100),
                           int(thr_pct * 10),
                           int(yaw_rate),
                           int(vel_x), int(vel_y), int(vel_z))
        self.send(0xFF, 0x41, data)

    # ── 内部接收循环 ─────────────────────────────────────────────

    def _recv_loop(self):
        while self._running:
            try:
                chunk = self._ser.read(512)
                if chunk:
                    self._buf.extend(chunk)
                self._parse_buf()
            except Exception:
                pass

    def _parse_buf(self):
        buf = self._buf
        while len(buf) >= 6:
            idx = buf.find(0xAA)
            if idx == -1:
                self._buf = bytearray()
                return
            if idx > 0:
                buf = buf[idx:]
            if len(buf) < 4:
                break

            ln   = buf[3]
            need = ln + 6
            if len(buf) < need:
                break

            frame = buf[:need]
            buf   = buf[need:]

            if verify_frame(frame):
                cmd     = frame[2]
                payload = bytes(frame[4:4 + ln])
                decoded = decode_frame(cmd, payload)
                ts      = time.monotonic()
                with self._lock:
                    self._frames[cmd] = (decoded, bytes(frame), ts)
                    self._stats[cmd] += 1
                # 触发回调
                for fn in self._callbacks.get(cmd, []):
                    try:
                        fn(decoded)
                    except Exception:
                        pass
            else:
                self._err_cnt += 1
                buf = buf[1:]   # 跳一字节重新同步

        self._buf = buf
```

---

## 7. 使用示例

### 7.1 基础：读取姿态和速度

```python
from ano_protocol import AnoProtocol
import time

proto = AnoProtocol('/dev/ttyAMA0', 500000)
# USB串口: AnoProtocol('/dev/ttyUSB0', 500000)
proto.start()

print("等待 IMU 数据...")
time.sleep(0.5)

while True:
    # 四元数帧（67 Hz，延迟最低）
    att = proto.get_frame(0x04, timeout=0.05)
    vel = proto.get_frame(0x07, timeout=0.05)
    alt = proto.get_frame(0x05, timeout=0.05)
    bat = proto.get_frame(0x0D)   # 低频，不等待

    if att:
        print(f"Roll={att['roll_deg']:+7.2f}°  "
              f"Pitch={att['pitch_deg']:+7.2f}°  "
              f"Yaw={att['yaw_deg']:+7.2f}°")
    if vel:
        print(f"Vx={vel['vel_x_cms']:+5d}  Vy={vel['vel_y_cms']:+5d}  "
              f"Vz={vel['vel_z_cms']:+5d} cm/s")
    if alt:
        print(f"Alt={alt['alt_fused_cm']} cm")
    if bat:
        print(f"Battery: {bat['voltage_v']:.2f}V  {bat['current_a']:.2f}A")
    
    time.sleep(0.05)   # 20Hz 读取循环
```

### 7.2 回调模式（最低延迟，推荐用于控制）

```python
from ano_protocol import AnoProtocol

proto = AnoProtocol('/dev/ttyAMA0', 500000)

latest_att = {}
latest_vel = {}

def on_attitude(d):
    latest_att.update(d)
    # 在这里直接做控制计算，每帧触发（67 Hz）
    roll  = d['roll_deg']
    pitch = d['pitch_deg']
    yaw   = d['yaw_deg']
    # your_control_loop(roll, pitch, yaw)

def on_velocity(d):
    latest_vel.update(d)

proto.register_callback(0x04, on_attitude)
proto.register_callback(0x07, on_velocity)
proto.start()

import time
while True:
    time.sleep(1)   # 主线程做其他事，回调自动在后台触发
```

### 7.3 查询电量

```python
import time
bat = proto.get_frame(0x0D, timeout=3.0)   # 电池帧约1秒一次，等3秒保证拿到
if bat:
    print(f"电压: {bat['voltage_v']:.2f} V")
    if bat['voltage_v'] < 10.5:
        print("⚠ 电量低！")
```

### 7.4 获取飞控状态（模式/解锁状态）

```python
sta = proto.get_frame(0x06, timeout=0.1)
if sta:
    print(f"模式: {sta['mode_str']}  解锁: {sta['unlocked']}")
```

### 7.5 发送 CMD 指令

```python
# 一键起飞（CID=0x10, CMD_0=0x03）
proto.send_cmd(0x10, 0x03)

# 一键降落
proto.send_cmd(0x10, 0x04)
```

### 7.6 统计帧率

```python
import time

stats_before, _ = proto.stats()
time.sleep(1.0)
stats_after, _ = proto.stats()

for cmd, cnt in stats_after.items():
    hz = cnt - stats_before.get(cmd, 0)
    from ano_protocol import FRAME_NAME
    print(f"0x{cmd:02X} {FRAME_NAME.get(cmd,'?'):<15s}: {hz} Hz")
```

---

## 8. 发送自定义帧

### 8.1 发送任意帧（通用方法）

```python
import struct

# 例：发送自定义 0x50 帧，DATA = [val1(s16), val2(u32)]
val1 = -100
val2 = 99999
data = struct.pack('<hi', val1, val2)   # 6字节
proto.send(dest=0xFF, cmd=0x50, data=data)
```

### 8.2 手动构造帧（不依赖库）

```python
def build_frame(dest, cmd, data):
    ln  = len(data)
    buf = bytearray([0xAA, dest, cmd, ln]) + bytearray(data)
    sc = ac = 0
    for b in buf:
        sc = (sc + b) & 0xFF
        ac = (ac + sc) & 0xFF
    buf += bytearray([sc, ac])
    return bytes(buf)
```

### 8.3 上报传感器数据（0x32 通用位置，供 IMU 融合）

如果树莓派运行了定位算法（视觉/UWB），可上报给 IMU：

```python
import struct

def send_position(proto, x_cm, y_cm, z_cm, quality=100):
    """上报位置给 IMU（0x32 帧），IMU 会融合进位置估计"""
    data = struct.pack('<iii B', x_cm, y_cm, z_cm, quality)
    proto.send(0x60, 0x32, data)   # 目标是 IMU (0x60)

def send_distance(proto, dist_cm):
    """上报测距（0x34 帧），IMU 用于辅助高度融合"""
    data = struct.pack('<i', dist_cm)
    proto.send(0x60, 0x34, data)
```

---

## 9. 未来扩展指引

### 9.1 增加新帧解析

在 `ano_protocol.py` 的 `decode_frame()` 函数中，在 `elif cmd == 0xE0` 之前
添加新的 `elif cmd == 0xXX` 分支即可：

```python
# 例：解析新增 0x50 帧
elif cmd == 0x50 and n >= 6:
    return {
        'val1': _s16(p, 0),
        'val2': _u32(p, 2),
    }
```

添加后无需其他修改，`proto.get_frame(0x50)` 立即可用。

### 9.2 增加帧名称

在 `FRAME_NAME` 字典中添加一行：

```python
0x50: 'My_Custom_Frame',
```

### 9.3 多帧同时订阅

```python
# 同时订阅姿态、速度、高度、状态、电池
for cmd in [0x04, 0x07, 0x05, 0x06, 0x0D]:
    proto.register_callback(cmd, lambda d, c=cmd: handle(c, d))

def handle(cmd, data):
    print(f"Received 0x{cmd:02X}:", data)
```

### 9.4 数据录制与回放

```python
import json, time

# 录制
records = []
def record(d):
    records.append({'t': time.time(), 'data': d})

proto.register_callback(0x04, record)
proto.start()
time.sleep(30)   # 录制30秒
proto.stop()
with open('attitude_log.json', 'w') as f:
    json.dump(records, f)
```

---

## 附录：快速接线核对清单

```
□ PD2 (STM32 UART5_RX) ── 树莓派 GPIO15 (RXD)     ✓
□ GND ─────────────────── 树莓派 GND                ✓  
□ 电平 3.3V，无需电平转换                             ✓
□ 波特率：500000 bps                                 ✓
□ 格式：8N1，无流控                                   ✓
□ 禁用树莓派蓝牙串口（/boot/config.txt dtoverlay=disable-bt） ✓
□ 优先使用 0x04（四元数，67Hz）而非 0x03（欧拉角，0.67Hz）  ✓
```

---

*文档版本：v1.0 | 基于凌霄匿名通信协议 V7 | STM32F407 飞控固件*
